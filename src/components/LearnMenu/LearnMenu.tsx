import type { SetPage } from "@/types/page";

type LearnMenuProps = {
  setPage: SetPage;
};

const LearnMenu = ({ setPage }: LearnMenuProps) => {
  return <div>LearnMenu</div>;
};

export default LearnMenu;
